/* Code generated by protoc-gen-synurang-ffi. DO NOT EDIT. */
/* Synurang generator: v0.7.2; proto SHA-256: 2999dda2e4525fefb3864474b9d6481393a8745ede7126711de88caa979ad452 */
/* C Native API Header for volvoxai.v1 */

#pragma once

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif


/* ═══════════════════════════════════════════════════════════ */
/* VolvoxAiService — Native C API                            */
/* ═══════════════════════════════════════════════════════════ */


/* Ping */
uint8_t* volvox_ai_ping(
    int32_t* out_len
);

/* GetCapabilities */
uint8_t* volvox_ai_get_capabilities(
    int32_t* out_len
);

/* LoadModel (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_load_model_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* CreateModel (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_create_model_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* UnloadModel */
uint8_t* volvox_ai_unload_model(
    const uint8_t* model_id, int32_t model_id_len,
    int32_t* out_len
);

/* GetModelInfo */
uint8_t* volvox_ai_get_model_info(
    const uint8_t* model_id, int32_t model_id_len,
    int32_t* out_len
);

/* InspectModel */
uint8_t* volvox_ai_inspect_model(
    const uint8_t* model_id, int32_t model_id_len,
    int32_t include_graph,
    int32_t include_tensors,
    int32_t include_weight_files,
    int32_t include_params_json,
    int32_t* out_len
);

/* SaveModelWeights (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_save_model_weights_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* AddModelTensor */
uint8_t* volvox_ai_add_model_tensor(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* tensor, int32_t tensor_len,
    int32_t* out_len
);

/* RemoveModelTensor */
uint8_t* volvox_ai_remove_model_tensor(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* name, int32_t name_len,
    int32_t* out_len
);

/* SaveTrainingCheckpoint (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_save_training_checkpoint_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* LoadTrainingCheckpoint (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_load_training_checkpoint_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* GetGraph */
uint8_t* volvox_ai_get_graph(
    const uint8_t* model_id, int32_t model_id_len,
    int32_t* out_len
);

/* ListModels */
uint8_t* volvox_ai_list_models(
    int32_t* out_len
);

/* InspectSafetensors */
uint8_t* volvox_ai_inspect_safetensors(
    const uint8_t* path, int32_t path_len,
    int32_t include_metadata,
    int32_t* out_len
);

/* CreateSafetensors (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_create_safetensors_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* StageAdapter (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_stage_adapter_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* LoadAdapter (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_load_adapter_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* ListAdapters (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_list_adapters_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* ActivateAdapter */
uint8_t* volvox_ai_activate_adapter(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* selection, int32_t selection_len,
    int32_t* out_len
);

/* UpdateAdapter (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_update_adapter_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* RemoveAdapter */
uint8_t* volvox_ai_remove_adapter(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* adapter, int32_t adapter_len,
    int32_t* out_len
);

/* MergeAdapter */
uint8_t* volvox_ai_merge_adapter(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* adapter, int32_t adapter_len,
    int32_t* out_len
);

/* UnmergeAdapter (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_unmerge_adapter_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* SaveAdapter */
uint8_t* volvox_ai_save_adapter(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* adapter, int32_t adapter_len,
    const uint8_t* path, int32_t path_len,
    int32_t atomic,
    int32_t* out_len
);

/* Run (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_run_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* GetTensor */
uint8_t* volvox_ai_get_tensor(
    const uint8_t* model_id, int32_t model_id_len,
    const uint8_t* name, int32_t name_len,
    int32_t* out_len
);

/* SetTensor (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_set_tensor_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* ApplyTensorUpdates (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_apply_tensor_updates_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* TrainStep (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_train_step_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Seq2SeqTrainStep (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_seq2_seq_train_step_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* TrainLoRaStep (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_train_lo_ra_step_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* PatchGraph (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_patch_graph_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Classify (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_classify_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* Detect (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_detect_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);

/* RecognizeCtc (protobuf input — has repeated/oneof fields) */
uint8_t* volvox_ai_recognize_ctc_pb(
    const uint8_t* data, int32_t data_len,
    int32_t* out_len
);


/* ═══════════════════════════════════════════════════════════ */
/* Memory Management & Error Retrieval                        */
/* ═══════════════════════════════════════════════════════════ */

/* Free memory returned by native API functions.              */
/* The allocation size is recovered from the prepended header. */
void volvox_ai_free(uint8_t* ptr);

/* Returns a pointer to the last error message (UTF-8, not    */
/* null-terminated). Valid until the next FFI call into this   */
/* module. Returns NULL if no error. Writes byte length to    */
/* out_len.                                                   */
const uint8_t* volvox_ai_last_error(int32_t* out_len);


#ifdef __cplusplus
}
#endif
