/* Generated by protoc-gen-synurang-ffi (mode=module). DO NOT EDIT. */
/* Synurang generator: 0.8.0; revision: 53180b484cf7ca07a1e7d6f24e58b8a19a2dcfa8; proto SHA-256: 5da3842c3c492794657b5cf40519d85ae253dd297c1f06fcf0b306219118eedf */
#pragma once
#include "volvoxai_lite.h"

#include <synurang/call.h>
#ifdef __cplusplus
extern "C" {
#endif


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlatformServiceGetPlatformInfoHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1Empty*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlatformServiceGetPlatformInfoHandler;
SynurangStatus vx_platform_get_platform_info_respond(
    SynurangStream*, const VolvoxaiV1PlatformInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlatformServiceDescribeApiHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DescribeApiRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlatformServiceDescribeApiHandler;
SynurangStatus vx_platform_describe_api_respond(
    SynurangStream*, const VolvoxaiV1ApiDescription*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlatformServiceGetMonotonicTimeHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1Empty*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlatformServiceGetMonotonicTimeHandler;
SynurangStatus vx_platform_get_monotonic_time_respond(
    SynurangStream*, const VolvoxaiV1MonotonicTime*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlatformServiceDescribeStatusHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DescribeStatusRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlatformServiceDescribeStatusHandler;
SynurangStatus vx_platform_describe_status_respond(
    SynurangStream*, const VolvoxaiV1StatusDescription*);

typedef struct VxPlatformServiceHandlers {

    VxPlatformServiceGetPlatformInfoHandler get_platform_info;

    VxPlatformServiceDescribeApiHandler describe_api;

    VxPlatformServiceGetMonotonicTimeHandler get_monotonic_time;

    VxPlatformServiceDescribeStatusHandler describe_status;

} VxPlatformServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_platform_register(SynurangInstance*,
    const VxPlatformServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceStartTraceHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1StartTraceRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceStartTraceHandler;
SynurangStatus vx_profiling_start_trace_respond(
    SynurangStream*, const VolvoxaiV1TraceInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceStopTraceHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TraceRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceStopTraceHandler;
SynurangStatus vx_profiling_stop_trace_respond(
    SynurangStream*, const VolvoxaiV1TraceInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceGetTraceHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TraceRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceGetTraceHandler;
SynurangStatus vx_profiling_get_trace_respond(
    SynurangStream*, const VolvoxaiV1TraceInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceReadTraceHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ReadTraceRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceReadTraceHandler;
SynurangStatus vx_profiling_read_trace_respond(
    SynurangStream*, const VolvoxaiV1TracePage*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceExportChromeTraceHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExportChromeTraceRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceExportChromeTraceHandler;
SynurangStatus vx_profiling_export_chrome_trace_respond(
    SynurangStream*, const VolvoxaiV1TraceChunk*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceReleaseTraceHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TraceRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceReleaseTraceHandler;
SynurangStatus vx_profiling_release_trace_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxProfilingServiceGetMemorySnapshotHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1GetMemorySnapshotRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxProfilingServiceGetMemorySnapshotHandler;
SynurangStatus vx_profiling_get_memory_snapshot_respond(
    SynurangStream*, const VolvoxaiV1MemorySnapshotResponse*);

typedef struct VxProfilingServiceHandlers {

    VxProfilingServiceStartTraceHandler start_trace;

    VxProfilingServiceStopTraceHandler stop_trace;

    VxProfilingServiceGetTraceHandler get_trace;

    VxProfilingServiceReadTraceHandler read_trace;

    VxProfilingServiceExportChromeTraceHandler export_chrome_trace;

    VxProfilingServiceReleaseTraceHandler release_trace;

    VxProfilingServiceGetMemorySnapshotHandler get_memory_snapshot;

} VxProfilingServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_profiling_register(SynurangInstance*,
    const VxProfilingServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTextServiceCreateTokenizerHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreateTokenizerRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTextServiceCreateTokenizerHandler;
SynurangStatus vx_text_create_tokenizer_respond(
    SynurangStream*, const VolvoxaiV1TokenizerHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTextServiceEncodeTextHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1EncodeTextRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTextServiceEncodeTextHandler;
SynurangStatus vx_text_encode_text_respond(
    SynurangStream*, const VolvoxaiV1EncodedText*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTextServiceDecodeTokensHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DecodeTokensRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTextServiceDecodeTokensHandler;
SynurangStatus vx_text_decode_tokens_respond(
    SynurangStream*, const VolvoxaiV1DecodedText*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTextServiceReleaseTokenizerHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TokenizerRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTextServiceReleaseTokenizerHandler;
SynurangStatus vx_text_release_tokenizer_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

typedef struct VxTextServiceHandlers {

    VxTextServiceCreateTokenizerHandler create_tokenizer;

    VxTextServiceEncodeTextHandler encode_text;

    VxTextServiceDecodeTokensHandler decode_tokens;

    VxTextServiceReleaseTokenizerHandler release_tokenizer;

} VxTextServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_text_register(SynurangInstance*,
    const VxTextServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceCreateRuntimeHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreateRuntimeRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceCreateRuntimeHandler;
SynurangStatus vx_inference_create_runtime_respond(
    SynurangStream*, const VolvoxaiV1RuntimeHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReleaseRuntimeHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RuntimeRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReleaseRuntimeHandler;
SynurangStatus vx_inference_release_runtime_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceListBackendsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RuntimeRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceListBackendsHandler;
SynurangStatus vx_inference_list_backends_respond(
    SynurangStream*, const VolvoxaiV1BackendList*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceLoadModelHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1LoadModelRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceLoadModelHandler;
SynurangStatus vx_inference_load_model_respond(
    SynurangStream*, const VolvoxaiV1ModelHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetModelInfoHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ModelRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetModelInfoHandler;
SynurangStatus vx_inference_get_model_info_respond(
    SynurangStream*, const VolvoxaiV1ModelInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetModelRevisionHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ModelRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetModelRevisionHandler;
SynurangStatus vx_inference_get_model_revision_respond(
    SynurangStream*, const VolvoxaiV1RevisionInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServicePublishAdapterHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1PublishAdapterRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServicePublishAdapterHandler;
SynurangStatus vx_inference_publish_adapter_respond(
    SynurangStream*, const VolvoxaiV1AdapterRevision*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReleaseModelHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ModelRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReleaseModelHandler;
SynurangStatus vx_inference_release_model_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceCompileModelHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CompileModelRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceCompileModelHandler;
SynurangStatus vx_inference_compile_model_respond(
    SynurangStream*, const VolvoxaiV1CompiledModelHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReleaseCompiledModelHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CompiledModelRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReleaseCompiledModelHandler;
SynurangStatus vx_inference_release_compiled_model_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceRunHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RunRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceRunHandler;
SynurangStatus vx_inference_run_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceCreateExecutionContextHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreateExecutionContextRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceCreateExecutionContextHandler;
SynurangStatus vx_inference_create_execution_context_respond(
    SynurangStream*, const VolvoxaiV1ExecutionContextHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReleaseExecutionContextHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutionContextRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReleaseExecutionContextHandler;
SynurangStatus vx_inference_release_execution_context_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetInputAffineQuantizationHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1GetInputAffineQuantizationRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetInputAffineQuantizationHandler;
SynurangStatus vx_inference_get_input_affine_quantization_respond(
    SynurangStream*, const VolvoxaiV1AffineQuantization*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceExecuteHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecuteRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceExecuteHandler;
SynurangStatus vx_inference_execute_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetTensorInteropInfoHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutionContextRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetTensorInteropInfoHandler;
SynurangStatus vx_inference_get_tensor_interop_info_respond(
    SynurangStream*, const VolvoxaiV1TensorInteropInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceExecuteTensorsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecuteTensorsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceExecuteTensorsHandler;
SynurangStatus vx_inference_execute_tensors_respond(
    SynurangStream*, const VolvoxaiV1TensorBatch*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceExecutePrefixHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutePrefixRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceExecutePrefixHandler;
SynurangStatus vx_inference_execute_prefix_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceDecodePrefillHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DecodePrefillRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceDecodePrefillHandler;
SynurangStatus vx_inference_decode_prefill_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceDecodeStepHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DecodeStepRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceDecodeStepHandler;
SynurangStatus vx_inference_decode_step_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceDecodeGenerateHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DecodeGenerateRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceDecodeGenerateHandler;
SynurangStatus vx_inference_decode_generate_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetDecodeStateHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutionContextRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetDecodeStateHandler;
SynurangStatus vx_inference_get_decode_state_respond(
    SynurangStream*, const VolvoxaiV1DecodeContextState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceResetDecodeHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutionContextRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceResetDecodeHandler;
SynurangStatus vx_inference_reset_decode_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceConfigureDecodeCacheHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ConfigureDecodeCacheRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceConfigureDecodeCacheHandler;
SynurangStatus vx_inference_configure_decode_cache_respond(
    SynurangStream*, const VolvoxaiV1DecodeCacheState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetDecodeCacheHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutionContextRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetDecodeCacheHandler;
SynurangStatus vx_inference_get_decode_cache_respond(
    SynurangStream*, const VolvoxaiV1DecodeCacheState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServicePublishDecodePrefixHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1PublishDecodePrefixRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServicePublishDecodePrefixHandler;
SynurangStatus vx_inference_publish_decode_prefix_respond(
    SynurangStream*, const VolvoxaiV1DecodeCacheState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReuseDecodePrefixHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ReuseDecodePrefixRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReuseDecodePrefixHandler;
SynurangStatus vx_inference_reuse_decode_prefix_respond(
    SynurangStream*, const VolvoxaiV1DecodeCacheState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReleaseDecodeLaneHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DecodeLaneRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReleaseDecodeLaneHandler;
SynurangStatus vx_inference_release_decode_lane_respond(
    SynurangStream*, const VolvoxaiV1DecodeCacheState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceEvictDecodePrefixesHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1EvictDecodePrefixesRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceEvictDecodePrefixesHandler;
SynurangStatus vx_inference_evict_decode_prefixes_respond(
    SynurangStream*, const VolvoxaiV1DecodeCacheState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceSelectAdapterHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1SelectAdapterRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceSelectAdapterHandler;
SynurangStatus vx_inference_select_adapter_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceRebindAdapterHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExecutionContextRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceRebindAdapterHandler;
SynurangStatus vx_inference_rebind_adapter_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceGetResultHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ResultRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceGetResultHandler;
SynurangStatus vx_inference_get_result_respond(
    SynurangStream*, const VolvoxaiV1ResultInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReadOutputHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ReadOutputRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReadOutputHandler;
SynurangStatus vx_inference_read_output_respond(
    SynurangStream*, const VolvoxaiV1ReadOutputResponse*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxInferenceServiceReleaseResultHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ResultRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxInferenceServiceReleaseResultHandler;
SynurangStatus vx_inference_release_result_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

typedef struct VxInferenceServiceHandlers {

    VxInferenceServiceCreateRuntimeHandler create_runtime;

    VxInferenceServiceReleaseRuntimeHandler release_runtime;

    VxInferenceServiceListBackendsHandler list_backends;

    VxInferenceServiceLoadModelHandler load_model;

    VxInferenceServiceGetModelInfoHandler get_model_info;

    VxInferenceServiceGetModelRevisionHandler get_model_revision;

    VxInferenceServicePublishAdapterHandler publish_adapter;

    VxInferenceServiceReleaseModelHandler release_model;

    VxInferenceServiceCompileModelHandler compile_model;

    VxInferenceServiceReleaseCompiledModelHandler release_compiled_model;

    VxInferenceServiceRunHandler run;

    VxInferenceServiceCreateExecutionContextHandler create_execution_context;

    VxInferenceServiceReleaseExecutionContextHandler release_execution_context;

    VxInferenceServiceGetInputAffineQuantizationHandler get_input_affine_quantization;

    VxInferenceServiceExecuteHandler execute;

    VxInferenceServiceGetTensorInteropInfoHandler get_tensor_interop_info;

    VxInferenceServiceExecuteTensorsHandler execute_tensors;

    VxInferenceServiceExecutePrefixHandler execute_prefix;

    VxInferenceServiceDecodePrefillHandler decode_prefill;

    VxInferenceServiceDecodeStepHandler decode_step;

    VxInferenceServiceDecodeGenerateHandler decode_generate;

    VxInferenceServiceGetDecodeStateHandler get_decode_state;

    VxInferenceServiceResetDecodeHandler reset_decode;

    VxInferenceServiceConfigureDecodeCacheHandler configure_decode_cache;

    VxInferenceServiceGetDecodeCacheHandler get_decode_cache;

    VxInferenceServicePublishDecodePrefixHandler publish_decode_prefix;

    VxInferenceServiceReuseDecodePrefixHandler reuse_decode_prefix;

    VxInferenceServiceReleaseDecodeLaneHandler release_decode_lane;

    VxInferenceServiceEvictDecodePrefixesHandler evict_decode_prefixes;

    VxInferenceServiceSelectAdapterHandler select_adapter;

    VxInferenceServiceRebindAdapterHandler rebind_adapter;

    VxInferenceServiceGetResultHandler get_result;

    VxInferenceServiceReadOutputHandler read_output;

    VxInferenceServiceReleaseResultHandler release_result;

} VxInferenceServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_inference_register(SynurangInstance*,
    const VxInferenceServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceSubmitHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1SubmitRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceSubmitHandler;
SynurangStatus vx_scheduler_submit_respond(
    SynurangStream*, const VolvoxaiV1RequestHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServicePollRequestHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RequestRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServicePollRequestHandler;
SynurangStatus vx_scheduler_poll_request_respond(
    SynurangStream*, const VolvoxaiV1RequestInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceWaitRequestHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RequestRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceWaitRequestHandler;
SynurangStatus vx_scheduler_wait_request_respond(
    SynurangStream*, const VolvoxaiV1RequestInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceCancelRequestHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RequestRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceCancelRequestHandler;
SynurangStatus vx_scheduler_cancel_request_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceTakeRequestResultHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RequestRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceTakeRequestResultHandler;
SynurangStatus vx_scheduler_take_request_result_respond(
    SynurangStream*, const VolvoxaiV1ExecutionResultHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceReleaseRequestHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1RequestRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceReleaseRequestHandler;
SynurangStatus vx_scheduler_release_request_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceCreateBatchQueueHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreateBatchQueueRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceCreateBatchQueueHandler;
SynurangStatus vx_scheduler_create_batch_queue_respond(
    SynurangStream*, const VolvoxaiV1BatchQueueHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceSubmitBatchWorkHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1SubmitBatchWorkRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceSubmitBatchWorkHandler;
SynurangStatus vx_scheduler_submit_batch_work_respond(
    SynurangStream*, const VolvoxaiV1BatchWorkHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceNextBatchDispatchHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BatchQueueRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceNextBatchDispatchHandler;
SynurangStatus vx_scheduler_next_batch_dispatch_respond(
    SynurangStream*, const VolvoxaiV1BatchDispatch*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceCompleteBatchDispatchHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CompleteBatchDispatchRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceCompleteBatchDispatchHandler;
SynurangStatus vx_scheduler_complete_batch_dispatch_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceGetBatchWorkHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BatchWorkRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceGetBatchWorkHandler;
SynurangStatus vx_scheduler_get_batch_work_respond(
    SynurangStream*, const VolvoxaiV1BatchWorkInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceTakeBatchWorkHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BatchWorkRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceTakeBatchWorkHandler;
SynurangStatus vx_scheduler_take_batch_work_respond(
    SynurangStream*, const VolvoxaiV1BatchWorkInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceGetBatchQueueHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BatchQueueRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceGetBatchQueueHandler;
SynurangStatus vx_scheduler_get_batch_queue_respond(
    SynurangStream*, const VolvoxaiV1BatchQueueInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceCancelBatchWorkHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BatchWorkRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceCancelBatchWorkHandler;
SynurangStatus vx_scheduler_cancel_batch_work_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceCloseBatchQueueHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CloseBatchQueueRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceCloseBatchQueueHandler;
SynurangStatus vx_scheduler_close_batch_queue_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxSchedulerServiceReleaseBatchQueueHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BatchQueueRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxSchedulerServiceReleaseBatchQueueHandler;
SynurangStatus vx_scheduler_release_batch_queue_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

typedef struct VxSchedulerServiceHandlers {

    VxSchedulerServiceSubmitHandler submit;

    VxSchedulerServicePollRequestHandler poll_request;

    VxSchedulerServiceWaitRequestHandler wait_request;

    VxSchedulerServiceCancelRequestHandler cancel_request;

    VxSchedulerServiceTakeRequestResultHandler take_request_result;

    VxSchedulerServiceReleaseRequestHandler release_request;

    VxSchedulerServiceCreateBatchQueueHandler create_batch_queue;

    VxSchedulerServiceSubmitBatchWorkHandler submit_batch_work;

    VxSchedulerServiceNextBatchDispatchHandler next_batch_dispatch;

    VxSchedulerServiceCompleteBatchDispatchHandler complete_batch_dispatch;

    VxSchedulerServiceGetBatchWorkHandler get_batch_work;

    VxSchedulerServiceTakeBatchWorkHandler take_batch_work;

    VxSchedulerServiceGetBatchQueueHandler get_batch_queue;

    VxSchedulerServiceCancelBatchWorkHandler cancel_batch_work;

    VxSchedulerServiceCloseBatchQueueHandler close_batch_queue;

    VxSchedulerServiceReleaseBatchQueueHandler release_batch_queue;

} VxSchedulerServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_scheduler_register(SynurangInstance*,
    const VxSchedulerServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceCreateGraphPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreateGraphPlanRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceCreateGraphPlanHandler;
SynurangStatus vx_planning_create_graph_plan_respond(
    SynurangStream*, const VolvoxaiV1GraphPlanHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceGetGraphPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1GraphPlanRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceGetGraphPlanHandler;
SynurangStatus vx_planning_get_graph_plan_respond(
    SynurangStream*, const VolvoxaiV1GraphPlanInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceEditGraphPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1EditGraphPlanRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceEditGraphPlanHandler;
SynurangStatus vx_planning_edit_graph_plan_respond(
    SynurangStream*, const VolvoxaiV1GraphPlanHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceExportGraphPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1GraphPlanRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceExportGraphPlanHandler;
SynurangStatus vx_planning_export_graph_plan_respond(
    SynurangStream*, const VolvoxaiV1ExportedGraphPlan*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceSerializeGraphHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1GraphDefinition*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceSerializeGraphHandler;
SynurangStatus vx_planning_serialize_graph_respond(
    SynurangStream*, const VolvoxaiV1SerializedGraph*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceResolveGraphPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ResolveGraphPlanRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceResolveGraphPlanHandler;
SynurangStatus vx_planning_resolve_graph_plan_respond(
    SynurangStream*, const VolvoxaiV1ResolvedGraphPlan*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceReleaseGraphPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1GraphPlanRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceReleaseGraphPlanHandler;
SynurangStatus vx_planning_release_graph_plan_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceInspectSafetensorsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1InspectSafetensorsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceInspectSafetensorsHandler;
SynurangStatus vx_planning_inspect_safetensors_respond(
    SynurangStream*, const VolvoxaiV1SafetensorsInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceReadSafetensorsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ReadSafetensorsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceReadSafetensorsHandler;
SynurangStatus vx_planning_read_safetensors_respond(
    SynurangStream*, const VolvoxaiV1SafetensorsContent*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxPlanningServiceWriteSafetensorsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1WriteSafetensorsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxPlanningServiceWriteSafetensorsHandler;
SynurangStatus vx_planning_write_safetensors_respond(
    SynurangStream*, const VolvoxaiV1SafetensorsArtifact*);

typedef struct VxPlanningServiceHandlers {

    VxPlanningServiceCreateGraphPlanHandler create_graph_plan;

    VxPlanningServiceGetGraphPlanHandler get_graph_plan;

    VxPlanningServiceEditGraphPlanHandler edit_graph_plan;

    VxPlanningServiceExportGraphPlanHandler export_graph_plan;

    VxPlanningServiceSerializeGraphHandler serialize_graph;

    VxPlanningServiceResolveGraphPlanHandler resolve_graph_plan;

    VxPlanningServiceReleaseGraphPlanHandler release_graph_plan;

    VxPlanningServiceInspectSafetensorsHandler inspect_safetensors;

    VxPlanningServiceReadSafetensorsHandler read_safetensors;

    VxPlanningServiceWriteSafetensorsHandler write_safetensors;

} VxPlanningServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_planning_register(SynurangInstance*,
    const VxPlanningServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceInitializeTensorHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1InitializeTensorRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceInitializeTensorHandler;
SynurangStatus vx_training_initialize_tensor_respond(
    SynurangStream*, const VolvoxaiV1InitializedTensor*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceBuildLoraLinearHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BuildLoraLinearRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceBuildLoraLinearHandler;
SynurangStatus vx_training_build_lora_linear_respond(
    SynurangStream*, const VolvoxaiV1LoraLinearPlan*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceBuildRoutedAdapterHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BuildRoutedAdapterRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceBuildRoutedAdapterHandler;
SynurangStatus vx_training_build_routed_adapter_respond(
    SynurangStream*, const VolvoxaiV1RoutedAdapterPlan*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceCreateTrainerHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreateTrainerRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceCreateTrainerHandler;
SynurangStatus vx_training_create_trainer_respond(
    SynurangStream*, const VolvoxaiV1TrainerHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceReleaseTrainerHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainerRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceReleaseTrainerHandler;
SynurangStatus vx_training_release_trainer_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceTrainStepHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainStepRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceTrainStepHandler;
SynurangStatus vx_training_train_step_respond(
    SynurangStream*, const VolvoxaiV1TrainStepResult*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceGetTrainStepHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainStepRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceGetTrainStepHandler;
SynurangStatus vx_training_get_train_step_respond(
    SynurangStream*, const VolvoxaiV1TrainStepResult*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceReadTrainerParametersHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ReadTrainerParametersRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceReadTrainerParametersHandler;
SynurangStatus vx_training_read_trainer_parameters_respond(
    SynurangStream*, const VolvoxaiV1TensorBatch*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceCommitTrainerHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainerRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceCommitTrainerHandler;
SynurangStatus vx_training_commit_trainer_respond(
    SynurangStream*, const VolvoxaiV1RevisionInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceRollbackTrainerHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainerRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceRollbackTrainerHandler;
SynurangStatus vx_training_rollback_trainer_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceExportTrainerWeightsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExportTrainerWeightsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceExportTrainerWeightsHandler;
SynurangStatus vx_training_export_trainer_weights_respond(
    SynurangStream*, const VolvoxaiV1TrainerWeights*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceGetTrainerStateHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainerRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceGetTrainerStateHandler;
SynurangStatus vx_training_get_trainer_state_respond(
    SynurangStream*, const VolvoxaiV1TrainerState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceResetTrainerAccumulationHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1TrainerRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceResetTrainerAccumulationHandler;
SynurangStatus vx_training_reset_trainer_accumulation_respond(
    SynurangStream*, const VolvoxaiV1TrainerState*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxTrainingServiceExportTrainerCheckpointHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExportTrainerCheckpointRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxTrainingServiceExportTrainerCheckpointHandler;
SynurangStatus vx_training_export_trainer_checkpoint_respond(
    SynurangStream*, const VolvoxaiV1TrainerCheckpointInfo*);

typedef struct VxTrainingServiceHandlers {

    VxTrainingServiceInitializeTensorHandler initialize_tensor;

    VxTrainingServiceBuildLoraLinearHandler build_lora_linear;

    VxTrainingServiceBuildRoutedAdapterHandler build_routed_adapter;

    VxTrainingServiceCreateTrainerHandler create_trainer;

    VxTrainingServiceReleaseTrainerHandler release_trainer;

    VxTrainingServiceTrainStepHandler train_step;

    VxTrainingServiceGetTrainStepHandler get_train_step;

    VxTrainingServiceReadTrainerParametersHandler read_trainer_parameters;

    VxTrainingServiceCommitTrainerHandler commit_trainer;

    VxTrainingServiceRollbackTrainerHandler rollback_trainer;

    VxTrainingServiceExportTrainerWeightsHandler export_trainer_weights;

    VxTrainingServiceGetTrainerStateHandler get_trainer_state;

    VxTrainingServiceResetTrainerAccumulationHandler reset_trainer_accumulation;

    VxTrainingServiceExportTrainerCheckpointHandler export_trainer_checkpoint;

} VxTrainingServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_training_register(SynurangInstance*,
    const VxTrainingServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceExportQuantizedTrainerWeightsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExportQuantizedTrainerWeightsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceExportQuantizedTrainerWeightsHandler;
SynurangStatus vx_quantization_export_quantized_trainer_weights_respond(
    SynurangStream*, const VolvoxaiV1QuantizedTrainerWeights*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceQuantizeWeightHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1QuantizeWeightRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceQuantizeWeightHandler;
SynurangStatus vx_quantization_quantize_weight_respond(
    SynurangStream*, const VolvoxaiV1QuantizedWeight*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceDequantizeWeightHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1DequantizeWeightRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceDequantizeWeightHandler;
SynurangStatus vx_quantization_dequantize_weight_respond(
    SynurangStream*, const VolvoxaiV1InitializedTensor*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceAuthorPtqTemplateHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1AuthorPtqTemplateRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceAuthorPtqTemplateHandler;
SynurangStatus vx_quantization_author_ptq_template_respond(
    SynurangStream*, const VolvoxaiV1PtqTemplateInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceCreatePtqPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CreatePtqPlanRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceCreatePtqPlanHandler;
SynurangStatus vx_quantization_create_ptq_plan_respond(
    SynurangStream*, const VolvoxaiV1PtqPlanHandle*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceReleasePtqPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1PtqPlanRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceReleasePtqPlanHandler;
SynurangStatus vx_quantization_release_ptq_plan_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceCalibratePtqPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CalibratePtqPlanRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceCalibratePtqPlanHandler;
SynurangStatus vx_quantization_calibrate_ptq_plan_respond(
    SynurangStream*, const VolvoxaiV1PtqCalibrationInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceInspectPtqPlanHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1PtqPlanRef*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceInspectPtqPlanHandler;
SynurangStatus vx_quantization_inspect_ptq_plan_respond(
    SynurangStream*, const VolvoxaiV1PtqPlanInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxQuantizationServiceWritePtqPackageHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1WritePtqPackageRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxQuantizationServiceWritePtqPackageHandler;
SynurangStatus vx_quantization_write_ptq_package_respond(
    SynurangStream*, const VolvoxaiV1PtqPackageInfo*);

typedef struct VxQuantizationServiceHandlers {

    VxQuantizationServiceExportQuantizedTrainerWeightsHandler export_quantized_trainer_weights;

    VxQuantizationServiceQuantizeWeightHandler quantize_weight;

    VxQuantizationServiceDequantizeWeightHandler dequantize_weight;

    VxQuantizationServiceAuthorPtqTemplateHandler author_ptq_template;

    VxQuantizationServiceCreatePtqPlanHandler create_ptq_plan;

    VxQuantizationServiceReleasePtqPlanHandler release_ptq_plan;

    VxQuantizationServiceCalibratePtqPlanHandler calibrate_ptq_plan;

    VxQuantizationServiceInspectPtqPlanHandler inspect_ptq_plan;

    VxQuantizationServiceWritePtqPackageHandler write_ptq_package;

} VxQuantizationServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_quantization_register(SynurangInstance*,
    const VxQuantizationServiceHandlers*, void* service);


/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceGetBufferInfoHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BufferHandle*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceGetBufferInfoHandler;
SynurangStatus vx_buffer_get_buffer_info_respond(
    SynurangStream*, const VolvoxaiV1BufferInfo*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceAllocateBuffersHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1AllocateBuffersRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceAllocateBuffersHandler;
SynurangStatus vx_buffer_allocate_buffers_respond(
    SynurangStream*, const VolvoxaiV1BufferHandles*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceRetainBuffersHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BufferRefs*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceRetainBuffersHandler;
SynurangStatus vx_buffer_retain_buffers_respond(
    SynurangStream*, const VolvoxaiV1BufferHandles*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceReleaseBuffersHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BufferRefs*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceReleaseBuffersHandler;
SynurangStatus vx_buffer_release_buffers_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceCopyTensorsHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1CopyTensorsRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceCopyTensorsHandler;
SynurangStatus vx_buffer_copy_tensors_respond(
    SynurangStream*, const VolvoxaiV1TensorBatch*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceBeginBufferAccessHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1BufferAccessRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceBeginBufferAccessHandler;
SynurangStatus vx_buffer_begin_buffer_access_respond(
    SynurangStream*, const VolvoxaiV1BufferAccess*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceEndBufferAccessHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1EndBufferAccessRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceEndBufferAccessHandler;
SynurangStatus vx_buffer_end_buffer_access_respond(
    SynurangStream*, const VolvoxaiV1OperationReport*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceImportDLPackHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ImportDLPackRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceImportDLPackHandler;
SynurangStatus vx_buffer_import_d_l_pack_respond(
    SynurangStream*, const VolvoxaiV1TensorBatch*);

/* All callbacks must return promptly. Retain/release the stream for work that
 * outlives a callback; message and options are borrowed. on_writable retries
 * responses rejected with WOULD_BLOCK. on_destroy owns per-call cleanup. */
typedef struct VxBufferServiceExportDLPackHandler {
    void* (*open)(SynurangStream*, const SynurangCallOptions*, void* service);
    void (*message)(SynurangStream*, const VolvoxaiV1ExportDLPackRequest*, void* call);
    void (*half_close)(SynurangStream*, void* call);
    void (*writable)(SynurangStream*, void* call);
    void (*cancel)(SynurangStream*, void* call);
    void (*destroy)(void* call);
} VxBufferServiceExportDLPackHandler;
SynurangStatus vx_buffer_export_d_l_pack_respond(
    SynurangStream*, const VolvoxaiV1DLPackExport*);

typedef struct VxBufferServiceHandlers {

    VxBufferServiceGetBufferInfoHandler get_buffer_info;

    VxBufferServiceAllocateBuffersHandler allocate_buffers;

    VxBufferServiceRetainBuffersHandler retain_buffers;

    VxBufferServiceReleaseBuffersHandler release_buffers;

    VxBufferServiceCopyTensorsHandler copy_tensors;

    VxBufferServiceBeginBufferAccessHandler begin_buffer_access;

    VxBufferServiceEndBufferAccessHandler end_buffer_access;

    VxBufferServiceImportDLPackHandler import_d_l_pack;

    VxBufferServiceExportDLPackHandler export_d_l_pack;

} VxBufferServiceHandlers;
/* Copies the table. service is borrowed until instance_destroy returns OK.
 * On failure, discard the instance; registration can be partially complete. */
int vx_buffer_register(SynurangInstance*,
    const VxBufferServiceHandlers*, void* service);

#ifdef __cplusplus
}
#endif
