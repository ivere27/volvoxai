# Exact, download-free-at-runtime development environment for onnx_oracle.py.
# Install deliberately before running the required oracle gate; the harness
# verifies these exact versions and never downloads models or dependencies.
numpy==2.2.6
onnx==1.22.0
onnxruntime==1.23.2
safetensors==0.8.0
